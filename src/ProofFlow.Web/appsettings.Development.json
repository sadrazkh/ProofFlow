{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "Demo": {
    // On, with a password, so that pressing F5 produces something to look at rather than an empty
    // sign-up form. The password lives in DemoAccount and the sign-in page prints it — which is
    // only true here: outside Development an empty setting stays empty and no account is made.
    "Seed": true
  }
}
