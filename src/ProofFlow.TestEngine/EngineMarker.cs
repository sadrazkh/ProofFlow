namespace ProofFlow.TestEngine;

/// <summary>
/// A handle on this assembly for reflection: the architecture tests and the node registry both
/// need to name it without depending on whichever concrete type happens to exist today.
/// </summary>
public static class EngineMarker;
